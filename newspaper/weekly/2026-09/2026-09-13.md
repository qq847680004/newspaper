本周总结：
一、AI 中台与 Agent 平台
1、把 Dify、RAGFlow 部署到阿里云服务器，内存控制在 12G 左右，配置全部改为 .env 动态管理。
2、完成可溯源 RAG 问答系统的细节完善与功能测试，讨论 AI 中台数据架构与 agent 机构组成。
二、询价中心功能开发
1、指挥概览完成 spec 拆分、开发与测试；管理分析看板前后端 spec 完善并落地开发测试。
2、询价单、草稿新增作废功能，生成订单按运输方式跳转对应进出口单，前后端 spec 已实施。
3、修复雪花算法 Long 值传前端被 int 截断的报错，生成规则约束后续 Api 出值。
三、邮箱询价数据获取调研
1、完成 163 邮箱批量获取调研，生成 IMAP 授权码与抓取规则，梳理风控限制。
2、对比 webhook Haraka 与 IMAP 两条增量获取路线，确认单企业邮箱 IMAP 接近零风控，方案按此收敛。
3、明确邮箱内容先全量 JSON 落表，再清洗、由 LLM 判断是否询价，最后进询价草稿。
四、工程规范与效率提升
1、完成新电脑开发环境搭建与原电脑项目、数据库迁移，开发环境与微服务部署到本地虚拟机。
2、让 Cursor 沉淀部署经验，理顺迁移与本地部署流程。

下周规划：
1、本地完成 Haraka webhook 或 IMAP 企业邮箱的部署与连通验证，确定抓取方式。
2、编写邮箱内容落表 spec（PostgreSQL 日期分区），实施清洗、落表、LLM 判断询价、写入草稿的定时任务链路。
3、配置本地 xxjob 调度环境，支撑邮箱抓取与落表任务运行。
4、完成线上 agent 环境调优，部署真实知识库并推进应用上线。

本周感想：
本周 Dify、RAGFlow 在阿里云跑稳，看板模块从 spec 走到开发测试。邮箱获取调研下来更确定：方案够用就好，矩阵邮箱那套成本和风控都不划算，先用企业邮箱 IMAP 跑通，把清洗到落表的链路做扎实再谈扩量。


====================第二版====================


本周总结：
一、AI 平台与 Agent 环境
1、Dify、RAGFlow 迁到阿里云服务器运行，内存压在 12G 上下，配置项统一抽到 .env，扩容或换机部署不用改代码。
2、可溯源 RAG 问答系统补齐细节后完成功能测试；同步理清 AI 中台数据架构与 agent 组成。
二、看板模块与询价功能开发
1、指挥概览输出开发 spec 并拆成 ticket，开发测试走完一轮。
2、管理分析看板前后端 spec 定稿后进入开发，实施与测试一并完成。
3、询价单、草稿加了作废功能，生成订单按运输方式分流到进口单或出口单，前后端 spec 与实现同步交付。
三、邮箱询价数据调研
1、163 邮箱批量获取调研完成，拿到 IMAP 授权码，抓取规则与风控限制整理成文。
2、增量获取在 webhook Haraka 与企业邮箱 IMAP 间比选，结论是单企业邮箱 IMAP 基本不触发风控，方案照此定。
3、落库路径明确：先全量 JSON 落表，再清洗和 LLM 判断是否询价，最后接询价流程生成草稿。
四、开发环境与工程效率
1、新电脑环境装齐，原电脑项目与数据库迁移完成，开发环境、项目微服务在本地虚拟机跑起来。
2、修掉雪花算法 Long 值传前端被 int 截断的报错，并立规则约束后续 Api 出值。
3、把部署过程交给 Cursor 沉淀经验，迁移与本地部署流程理顺。

下周规划：
1、本地部署 Haraka webhook 或 IMAP 企业邮箱并打通连通性，把抓取方式定下来。
2、编写邮箱内容落表 spec（PostgreSQL 日期分区），实现清洗、落表、LLM 判断询价、写入草稿这条定时任务链。
3、配好本地 xxjob 调度环境，让邮箱抓取与落表任务能跑起来。
4、线上 agent 环境调优收尾，部署真实知识库，推进 agent 应用上线。

本周感想：
这周把 Dify、RAGFlow 挪到阿里云并统一 .env 配置，看板模块从 spec 做到开发测试。邮箱询价比选结论清楚：单企业邮箱 IMAP 就够用，矩阵邮箱成本高、风控也复杂，属于过度设计；先把抓取和落表链路跑通，再说扩量。

